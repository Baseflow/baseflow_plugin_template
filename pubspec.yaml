name: baseflow_plugin_template
description: A template for plugin examples
version: 1.0.0
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^5.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter

#flutter:
#  assets:
#    - lib/assets/logo.png
